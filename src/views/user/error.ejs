<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error | WA Campaign</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: { extend: { colors: { brand: { 500:'#22c55e', 600:'#16a34a', 700:'#15803d' } } } }
    }
  </script>
</head>
<body class="bg-gray-100">
<div class="max-w-sm mx-auto min-h-screen bg-gray-50 shadow-xl relative">
  <div class="flex flex-col items-center justify-center min-h-screen px-6">
    <div class="text-center">
      <div class="text-7xl mb-4"><%= statusCode === 404 ? '🔍' : '💥' %></div>
      <h1 class="text-3xl font-black text-gray-800 mb-2">Oops! Error <%= statusCode %></h1>
      <p class="text-gray-500 mb-6"><%= message %></p>
      <a href="/home" class="inline-flex items-center gap-2 px-6 py-3 bg-gradient-to-r from-brand-500 to-brand-600 text-white rounded-xl font-bold hover:shadow-lg hover:shadow-brand-500/25 transition-all active:scale-95">
        ← Kembali ke Home
      </a>
    </div>
  </div>
</div>
</body>
</html>
